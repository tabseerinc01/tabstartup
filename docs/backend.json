
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents an individual user profile (Founder, Mentor, or Investor) in TabStartup.",
      "properties": {
        "uid": { "type": "string" },
        "fullName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string" },
        "primaryRole": { "type": "string" },
        "roles": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "fullName", "email", "createdAt", "updatedAt"]
    },
    "Contact": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Contact",
      "type": "object",
      "description": "A private professional contact managed by a user.",
      "properties": {
        "ownerUid": { "type": "string" },
        "contactName": { "type": "string" },
        "companyName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "type": { "type": "string" },
        "notes": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerUid", "contactName", "createdAt", "updatedAt"]
    },
    "Deal": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Deal",
      "type": "object",
      "description": "A sales or investment deal in the user's pipeline.",
      "properties": {
        "ownerUid": { "type": "string" },
        "contactId": { "type": "string" },
        "contactName": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "value": { "type": "number" },
        "currency": { "type": "string", "default": "USD" },
        "stage": { "type": "string", "enum": ["Lead", "Contacted", "Proposal Sent", "Won", "Lost"] },
        "probability": { "type": "number" },
        "expectedCloseDate": { "type": "string", "format": "date" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerUid", "title", "stage", "createdAt", "updatedAt"]
    },
    "DealNote": {
      "title": "DealNote",
      "type": "object",
      "description": "An internal note attached to a deal.",
      "properties": {
        "content": { "type": "string" },
        "authorName": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "DealActivity": {
      "title": "DealActivity",
      "type": "object",
      "description": "A log of changes to a deal.",
      "properties": {
        "type": { "type": "string" },
        "details": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Task": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Task",
      "type": "object",
      "description": "A workspace task or reminder.",
      "properties": {
        "ownerUid": { "type": "string" },
        "dealId": { "type": "string" },
        "contactId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "In Progress", "Completed"] },
        "priority": { "type": "string", "enum": ["Low", "Medium", "High"] },
        "dueDate": { "type": "string", "format": "date-time" },
        "completedAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerUid", "title", "status", "priority", "createdAt", "updatedAt"]
    },
    "TaskActivity": {
      "title": "TaskActivity",
      "type": "object",
      "description": "A log of changes to a task.",
      "properties": {
        "details": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": { "entityName": "User", "schema": { "$ref": "#/backend/entities/User" }, "description": "User profile data." }
      },
      {
        "path": "/contacts/{contactId}",
        "definition": { "entityName": "Contact", "schema": { "$ref": "#/backend/entities/Contact" }, "description": "Private contact records." }
      },
      {
        "path": "/deals/{dealId}",
        "definition": { "entityName": "Deal", "schema": { "$ref": "#/backend/entities/Deal" }, "description": "Private pipeline deals." }
      },
      {
        "path": "/deals/{dealId}/notes/{noteId}",
        "definition": { "entityName": "DealNote", "schema": { "$ref": "#/backend/entities/DealNote" }, "description": "Internal notes for a deal." }
      },
      {
        "path": "/deals/{dealId}/activities/{activityId}",
        "definition": { "entityName": "DealActivity", "schema": { "$ref": "#/backend/entities/DealActivity" }, "description": "History of changes for a deal." }
      },
      {
        "path": "/tasks/{taskId}",
        "definition": { "entityName": "Task", "schema": { "$ref": "#/backend/entities/Task" }, "description": "Private workspace tasks." }
      },
      {
        "path": "/tasks/{taskId}/activities/{activityId}",
        "definition": { "entityName": "TaskActivity", "schema": { "$ref": "#/backend/entities/TaskActivity" }, "description": "History of changes for a task." }
      }
    ]
  }
}
